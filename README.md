# not-a-go-repo

No `go.mod` anywhere — just a plain text file. Not a Go module/repo at all.

Validates graceful handling of non-Go repositories (no crash, no panic).

Expected: `IsVulnerable=unknown`, `Files={}`, `UsedImports={}`.
